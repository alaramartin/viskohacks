REACTOR_API_KEY=
# Backend (FastAPI) keys. Copy to .env and fill in. Never commit .env.
# Imagery: Mapillary only (no Google fallback).
MAPILLARY_ACCESS_TOKEN=
# Comma-separated origins allowed by backend CORS
CORS_ORIGINS=http://localhost:3000
